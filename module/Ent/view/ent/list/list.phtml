<?php $this->headTitle($this->translate('Liste des items des listes')); ?>

<h1><?= $this->translate('Liste des items des listes') ?></h1>

<p>
    <a href="<?= $this->url('zfcadmin/list/add', array('action' => 'add')); ?>" class="btn btn-primary btn-xs"><?= $this->translate('Ajouter un nouvel item de list') ?></a>
</p>

<?php foreach ($this->flashMessenger()->getSuccessMessages() as $m) : ?>
    <div class="alert alert-success" role="alert">
        <?= $this->translate($m) ?>  
    </div> 
<?php endforeach; ?>
<div class="input-group col-lg-3 pull-right">
    <input id="filterTable" type="text" class="form-control" placeholder="<?= $this->translate('Catch Me If You Can') ?>">
    <span class="input-group-addon"><?= $this->translate('Cheat !') ?></span>
</div>
<table class="table table-striped" >

    <!--           data-detail-view="true"
               data-detail-formatter="detailFormatter"-->
    <thead>
        <tr>
            <th><?= $this->translate('Item Label') ?></th>
            <th><?= $this->translate('Type List') ?></th>
            <th>&nbsp;</th>
            <th>&nbsp;</th>
            <th>&nbsp;</th>        
        </tr>
    </thead>
    <tbody  class="searchable">
        <?php foreach ($lists as $list) : ?>
            <tr>
                <td>
                    <?= $this->escapeHtml($list->getListLibelle()) ?>
                </td>
                <td>
                    <?= $this->escapeHtml($list->getFkListType()->getListtypeLibelle()) ?>
                </td>
                <td>
                    <a href="<?= $this->url('zfcadmin/list/show', ['id' => $this->escapeUrl($list->getListId())]) ?>" class="btn btn-primary btn-xs"><?= $this->translate('Détails') ?></a>
                </td>
                <td>
                    <a href="<?= $this->url('zfcadmin/list/show/update', ['id' => $this->escapeUrl($list->getListId())]) ?>" class="btn btn-warning btn-xs"><?= $this->translate('Modifier') ?></a>
                </td>
                <td>
                    <a href="<?= $this->url('zfcadmin/list/show/delete', ['id' => $this->escapeUrl($list->getListId())]) ?>" class="btn btn-danger btn-xs"><?= $this->translate('Supprimer') ?></a>
                </td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>
